import { MikroORM, type Options, type Configuration, type IDatabaseDriver } from '@mikro-orm/core';
import { MongoDriver } from './MongoDriver';
/**
 * @inheritDoc
 */
export declare class MongoMikroORM extends MikroORM<MongoDriver> {
    private static DRIVER;
    /**
     * @inheritDoc
     */
    static init<D extends IDatabaseDriver = MongoDriver>(options?: Options<D> | Configuration<D>, connect?: boolean): Promise<MikroORM<D>>;
}
export type MongoOptions = Options<MongoDriver>;
export declare function defineMongoConfig(options: MongoOptions): Options<MongoDriver>;
